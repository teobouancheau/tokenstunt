---
name: context
description: Symbol dependency graph — what it calls and what calls it
argument-hint: <symbol>
---

Call the ts_context MCP tool for the symbol "$ARGUMENTS". Show its definition, what it calls (dependencies), and what calls it (dependents).